---
title: "Typst Math Conversion Test"
lang: "en-US"
math: typst
---

# Typst Math Conversion Test

Here is the quadratic formula in Typst math block style:
$$ x = (-b +- sqrt(b^2 - 4 a c)) / (2 a) $$

Here is inline Typst math: $a^2 + b^2 = c^2$.

Here is the time-dependent Schrödinger equation:
$$ i planck.reduce (partial Psi) / (partial t) = (-planck.reduce^2 / (2 m) nabla^2 + V) Psi $$

Here is a contour integral test:
$$ integral.cont_gamma f(z) d z = 2 pi i $$
